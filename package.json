{
  "name": "@tlan16/toy-robot-simulator",
  "version": "1.0.0",
  "main": "src/main.ts",
  "repository": "git@github.com:tlan16/Toy-Robot-Simulator.git",
  "author": "Frank Lan <flan@msts.com>",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "build": "tsc -p tsconfig.build.json",
    "test": "jest --config jest.config.json --coverage",
    "lint": "eslint 'src/**/*.ts' 'tests/**/*.ts' --cache",
    "start": "yarn build && node dist/main.js"
  },
  "devDependencies": {
    "@tlan16/eslint-config-typescript": "^0.0.5",
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.9",
    "eslint": "^7.13.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.6",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.2"
  }
}
